Hi everyone! Welcome to this video. We've already in the past weeks discussed what agents are, how they work, and essentially how the large language models become the central piece of an agent, and how we were surprised about the reasoning capabilities of large language models. We got overexcited about this and said, "Hey, if these large language models can reason so well, why not give them, give this brain, the large language models, some hands, meaning tools, and move from what we wanted in the first place, which was language completion, to what we now want, which is task completion. So we built large language models as language completers, but now we want these agents as task completers. So in this video, we're going to dig more into the tools. We're going to talk about why we need to endow the agents with memory, and what does that buy us? And we'll also talk more about what these MCP standards are, and what this allows us to do, or it allows our agents to do.

Transcribed by https://otter.ai
So the central piece, as we've discussed already, is this intelligence problem. We've got intelligence, right? These large language models are seemingly intelligent, or they're extremely well able to fake intelligence, but the problem is they are completely independent. They are in a jar. You can talk to them; they can respond, but they cannot do stuff, and that's what we're trying to fix by introducing tools. Tools are actuators. It doesn't. If I ask my large language model to send an email, it can't. It can compose an email for me, but it literally can't hit the send button on my email client. The send button, in turn, on my email client, ends up calling maybe a Python function, and that Python function will take the contents of my email, talk to a mail transfer server and hand over the mail. The problem is that the large language model not only can't hit the send button, it can't even call that function directly. So we are hoping to give these tools, meaning these kinds of functions, to the large language model and say, hey, these are the functions. Feel free to call them with the right set of parameters when you want those functions to be called. So we're essentially endowing large language models with these hands. We're saying, here are these hands, the tools, and here's how they work. You are a reasoning engine. You are a brain. You know how to send messages to them. You know how to create a function call, which my other Python program, that's my system, can execute for you and hand over the result back to you. That's essentially how the tools work. Now, these tools are usually just function calls. Think of Python functions that are called, and it allows this brain to call these tools or functions and do stuff in the world: trade buy stock, buy more stock, sell more stock, send an email, right? Plan a trip, book a trip, whatever. But plan a trip would still be a large language model. I can plan a trip, but a planning and booking would be the task of an agent. Now, what I want to do is first I want to look a little more closely in how these tools are actually called right? Remember, these large language models are nothing more than language completion. They happen to have some amount of reasoning capability, and we want to take advantage of that and have the tools work with this reasoning capability. Tools are Python scripts, it could be any language scripts, but these are scripts or computer programs, and LLM is just a text generated, no hands, cannot press run on a Python script. The magic really is in the LLM understanding the list of tools and creating function calls in the right format. So, if I tell the large language model I've got these four different functions, and describe the four different functions, the hope is this brain can understand what functions I have handy, and when a task comes in, it can create a function call not only with the name of the function but the right set of parameters in the right order, and hand it over to the system, the LLM cannot run that script. They hand it over to the system, and the system will call the function with the right set of parameters, execute the function call, take the result, and hand it over to the LLM. That's what the way it works. Now let's go through one example. So let's say we create our first agent. An agent is nothing more than an agent prompt. So this this whole box is the agent. An agent has a few things. It's got an agent prompt. It's got an LLM brain. LLM brain, and it's got a user sending a query, and it needs to respond. And it needs to respond. The issue is, if not for the agent, I could say tell the, I could ask the agent an example: What is the weather?

In Austin right now,

right. So the right now is important. I'm asking the question: What's the weather in Austin right now? And remember, without any. Of this agentic framework, the large language model has no clue. The large language model was trained as a language completion model with data from long, long ago. So currently, the weather in Austin is not something the large language model understands. Ideally, it would like to talk to a database, a weather database, and send in the name of the city and get back the weather details. Basically, maybe in a JSON format because it's a script, and then use a script to answer. But the problem is the LLM does not have an ability to talk to the database that contains the weather. So this is why we built an agentic framework. The agentic framework starts by saying it tells the agent prompt your helpful assistant, meaning kind of give you gives it a persona to essentially say you need to answer the questions in a nice polite manner. You have access to a tool, one tool in this case called Get Weather. Okay, Get Weather is essentially a script. Okay, so let's say there is a get weather script def get underscore weather and that's a script. It's got a whole bunch of lines and it's got a return string string. It's a string that's return after a whole and this get weather script probably it's a Python function. This get weather script probably logs into a database server, which contains the weather information, gets it, blah blah blah. This get weather script takes a city name, right? City is required, so all that you need to do is get weather and call it with one argument, which is the name of city in this case Austin, but this script cannot be fed in this entire English language, and this script wouldn't understand. This is a deterministic script. It takes the city, logs into a database, sends a SQL query with the name of city, gets a return, and sends that return as a string. Right? That's it. Now the issue is that LLMs don't know this script exists, so the prompt says you have access to a tool called GetWeather, which takes one argument city as a string. If you need to use it, output a JSON in this format. Okay. Now what we have is going to be going to have an agent executor. Agent, an agent executor. This is a program, and or remember, all of these form the agent system. So this is our core LLM brain. Think of it as an API call to a large language model, that's fine. So, and this system prompt, this agent prompt, is also fed into the LLM before. So this is the first prompt that goes in; it stays in. LLM learns from it, and now you ask the weather in Austin right now. This LLM starts to reason. It combines these two and says okay. It turns out I need to make a JSON. I need to make a call to get weather, right? And one argument is a city, and I've already said if you need this, make a JSON. So this is going to create a JSON request, and that JSON request is sent by the agent orchestrator or the computer program or the Python script that's running this entire agent or implementing this agent, and the JSON script goes action is get weather and action input the action input.

Input is the city Austin

and bracket close right. So again, apologies for the small font, but you get the idea. The idea is that it uses a format exactly and says the action is get weather. And the reason we need this is particularly because there might be a future where we have more than one tool, right? So we need the name of the tool, and we need the set of inputs the function needs or the tool needs. In this case, just city and Austin. And this agent executer is a computer program that has the ability to use this JSON, create a function call, and actually call the program. Right. So this this runs a call. Okay. This is a JSON, and this is this would be a JSON in this example, and this will be a JSON RPC probably remote procedure call. This is just another protocol for it to call a function. Maybe not if it is on a different computer, it's a JSON RPC. If not, it's just a computer call within the same machine. But it's it. The way it's this agent executor makes a call, this function executes itself, executes and returns maybe 7272 degrees sunny or something like that. That's a string return, and this string is also returned back to the LLM. 72 degrees sunny, and this LLM now needs the information. It has the information it needs and makes a language, English language-based interpretation of this text and this question and this thing and response the weather, The

in Austin right now

is 72 degrees Fahrenheit and it's sunny. Right, so that's English responded with good English. Now the problem with these computer programs is that they can't understand English. And even if you did make them understand a specific kind of English, I change the question to something a little different. I say, "Hey, I feel like going out right now. What do you think the weather is in Austin right now? Or something like that. I just change English a little bit up, and this will this will trip. But the large language model, since it understands English, will not trip as long as the context and the question revolves around requiring the weather in Austin. And once this large language model understands what tools it has, the big magic here is this language model can translate this question to this JSON, depending upon what tools are available. That translation is what ability to translate into a clear JSON call or clear function call is what allows the agent to use these tools efficiently. Now you can easily imagine instead of having one tool, I could have a whole bunch of tools. I could have tool number two, tool number three, tool number four, tool number five, tool number six, tool number seven, and just list out all the tools. Call you you have access to a set of tools and list the tools out instead of get weather. I just list all the tools out, and suddenly this large language model not only can use the weather, get the weather. It can actually book plane tickets. It can trade stock. It can do complex mathematical calculations. It can send emails. It can do a whole bunch of things just because of that tool. Without any of this, without the agent prompt or without any of this interface that allows the LLM to actually run Python code, without any of that, the LLM is just a brain in a jar. With all of these, all of a sudden the LLM can complete tasks by actually doing stuff in the real world.

Transcribed by https://otter.ai
Now let's talk a little about where these tools live. In this example, in this example, I said these tools probably live in the computer. In which case, this is just a agent just calling the function. You know, loading up the library and calling the function, but as I said, these tools might be available on a completely different server, and this is especially true in a complex world. We are doing complex tasks currently. Computers call each other using APIs. For example, if I want the map or a route from point A to B, I'm not going to build the entire Google Maps infrastructure by myself. I don't have the data. I don't need to do it. So I could simply make an API call to Google Maps. I could ask Google Maps server, "Hey, can you give me the driving directions from point A with this address to point B with this address? I could send that as an API call, and Google servers will respond to me with the address. I give sorry with the driving directions in a JSON format. Now I could use the JSON format, and in my own app, I could parse it and I could present it in a convenient way for my user, and these API calls are how computers have been talking to each other, and these API calls are quite brittle, right? Think about it. I version one of Google Maps are available, so I look at the Google API documentation, and I built my app using that documentation to make a call in a specific structured way. I make the call. The argument one needs to be this. Argument two needs to be this. Argument three needs to be this. Argument four needs to be my API key, and so on. Now, my app is built. Google's version one of that API is done, so we are talking to each other. Good. Suddenly, one fine day, Google decides they need to make a better API set because the features have increased. They come up with version two of their APIs, and all of these have changed. Argument three is no longer optional. Argument four was not what it used to be now it's argument two and newer arguments have come in new API whatever it's all a different system it's a now that forces me to rebuild my app I need to go I need to create a different version of my app I need to find out how the APIs have changed I need to change my code and implement it that's how the system has been working till today. Now the hope is that with English instead of APIs, with English being the mode of communication between computers and functions on computers, maybe there is a better way to do things. Maybe there is a way to architect the whole system such that when Google changes its API, my computer, my app doesn't need to be rewritten because my app now has an English understanding brain. This large language model can read the new API parameters list from Google and reframe its JSON call according to that parameter list dynamically without me having to rewrite code. That is the grand hope. Let's see how that is accomplished with what people called the MCP server. So this is the integration night pair. So tools are often living in different servers. Google APIs is the example I gave you, but there could be other APIs too. And one app like mine could be using a bunch of other servers, other APIs, and other tools. Right? An agent might need to call these tools to get a job done. And the whole point is, since this agent has an LLM that's intelligent or understands English, it might be best to allow the agent to learn the list of tools dynamically. So, if a tool gets added by the server, if a tool gets changed by the server, if a tool gets you know updated by the server, doesn't matter as long as the agent can learn the list of tools dynamically, we are in business, and we don't have to rebuild our app over and over again. So this is what a new protocol called the Model Context Protocol allows us to do. We'll go through it. It's an open standard for tools and agent integration. Agents to talk to tools.

It it's it's it's based on the client-server architecture. It decouples reasoning from execution, and it's best illustrated using an example. Let's go over an example. So here we have another agent. We call it the MCP host because this is essentially, you know. Model context protocol client with an agent. So the moment I say agent, you got a LLM that forms the central brain of this agent. Now we're going to have this question. We're going to start with an example. So let's say I ask the question: How many? How many iPhones do we have in stock? Okay, let's say I'm a retailer, I am Best Buy, and I all of a sudden ask this agent, right? How many iPhones do we have in stock? Because there's somebody up here asking me whether they can buy a dozen iPhones, and I need to check inventory, right? I ask in plain English, how many iPhones do we have in stock to this agent? To this agent, this agent is supposed to be my assistant or helper, and this agent needs to talk to my manufacturers or distributors or my warehouses database system. Now I don't have. Let's think this is somebody else. This is my distributor, like Google providing a map API. My distributor was providing an API before, but now my distributor is providing an MCP server that allows me to dynamically learn stuff. So this is my distributor, just like the Google example earlier. This is my distributor providing an MCP server. This is my agent. My these guys have access to this information, but I don't. But my LLM needs to talk to them. So the idea is, when this is the system, this is the entire system. This is the brain, right? The brain does not know all of this. But when the system gets initialized, when the entire agent system get MCP host with the agent gets initialized, the first thing that happens is during initialization before any of this prompting starts, the the server talks to the MCP server and does a bunch of things. First, it does authentication. It kind of establishes a handshake and say, "Hey, I'm authorized to talk to you, and you're authorized. Here's my username and password. You're authorized to whenever I ask you a question, you're authorized to send me information, blah blah blah, handshakes happen. Then after the handshake happens, the handshake happens, and the system says authorized. Okay. After the handshake, the next thing that does for an MCP host, the MCP host always starts with this function question list Tools. So this is a question saying, "Hey, I've authenticated. Now I have the right to talk to you. Can you send me the list of tools you have? The MCP servers respond, and that's what MCP protocol stands for. These are the ways we will communicate. If I ask you this, you need to respond with this and so on and so forth, so the MCP server responds saying, "Okay, I have the following set of tools. I have get table schema or get database schema. I have query DB. I have these two functions, okay, and it describes the functions. It describes the parameters of the functions, and it sends all that information back to initialization. Initialization is done. Then we move on to agent prompt. Agent prompt is created. You are helpful assistant. You have access to the following set of tools, and these tools are populated here. All the tools are listed with their descriptions. Once this agent prompt prompt is created, the agent prompt is fed to the large language model, and the large language model reads the agent prompt and says, "Okay, I'm ready to start execution. Now, once that execution begins, you ask the question, "How many iPhones do we have in stock? The large language model interprets this question, just like the earlier example. Looks at this list of tools and it decides that it needs to first ask, understand what the database structure looks like using get table schema or get database schema. Once it understands the database structure, it can create another SQL query, say select whatever in the whatever table and find me how many counts there are or whatever, right? Depending upon the table schema, it can actually come up with an SQL query. Now the way we do that is there is MCP clients that sits here.

Let's call it MCP client. MCP client is your agent executor, but in this case, it's going to call functions in a different server. So here, the LLM is going to create a JSON call. It's going to be the same way, action and. Action get table schema action inputs whatever inputs need to be there a JSON call and this JSON call is converted to a JSON RPC again just to remote procedure call that's a remote procedure call it lands on the server and the server responds saying. "Hey, here are the databases I have. Here are the tables I have. Here are the structure of each of the tables. Blah blah blah. And then that goes back to the large language model. The large language model then realizes, "Okay, it's got the names of the tables. It's got the names of the columns. It's got everything. Using all of that information, it can format another SQL query and call query DB. So it sends another another call, JSON call that says action Query DB action input the query and gives the SQL query. The MCP client converts that to JSON RPC, sends it back to the server, gets a response, hands it over, and the large language model responds. We currently have five iPhones in stock, or something like that. Okay, so that's it. Now here's the key. First, these tools completely lived on a different server, and still our agent was able to do this. But the very critical thing that I alluded to earlier, if my distributor were to change any of their implementations. Maybe suddenly they decide to call get underscore table underscore schema without the underscores. They decided for some reason that they want to call it get. They want to call use camel case instead of underscores in between. It's okay. I don't have to rewrite any part of the code during the Discovery process, my server will learn that there is a new tool, and it will allow, and that will paste the tool and its description here, and my LLM will automatically understand. Maybe there is another new function that my distributor is going to create. Maybe in the same function the distributor wants the API key every single time. Maybe, maybe, maybe my distributor can do anything and everything they want. They can change it from version one to 1.1 to 1.2 to 1.3 every minute of the day. But my agent, during every initialization, all I have to do is just reinitialize. During every initialization, we'll learn the list of tools, adjust its function call to match what it learned, and make the function call and respond. The beautiful part of the story is I don't need to rewrite my code crazy number of times because I'm letting this brain of mine, kind of dynamically readjust to the world that might be changing on me, and this becomes very, very. This becomes much more appreciated the moment you realize that your computer programs are talking to 20 different vendors, 20 different APIs, 20 different distributors, 20 different, and all of them, any given day, is going to make one change. And if that's the case, all you will be doing is creating different versions of your computer program over and over and over and over again. But with this kind of framework, you implement it once. You sit back and watch and monitor as long and see it adapting. Now, this dynamism is the key idea here. The learning and the execution are both dynamic.

Transcribed by https://otter.ai
Now I want to move on to the other topic for this lecture, which would be to talk about memory. Now, large language models are completely memoryless. So the funny part is when you open up ChatGPT or Gemini or whatever your favorite large language model is, and you start typing a query. You ask it a query, and you say, "Hey, I live in Austin. The large language model responds, "Break something, blah blah blah. And then you say, "What's the weather in my city right now? It seems to understand, you know, you're asking for weather in Austin because you just told it that you live in Austin, so that kind of gives us the feeling that it knows, it has a little bit of memory, and that memory is really, really not memory of the large language model. It's what we call the context window. So let me give you an example. Right, let me start with the example. So let's say you go to a large language model and say, "Who is the secret identity of Batman? and it responds, "Bruce Wayne. Then you say, "Who plays him in the Dark Knight? and it says Christian Bale. Now, when you ask the second question, it seems like the large language model had memory, and it remembered that you were talking about Batman and Bruce Wayne, right? So we are asking. In this question is interpreted as who plays Bruce Wayne in The Dark Knight. But really, really, what's going on is when this query was asked, the large language model. So this query was sent to the large language model, and it responded with Bruce Wayne. When this query is being sent, what your interface, your browser interface, did is not sending this query. It's sending this whole thing again into the large language model. And for this whole thing as your new query, this whole thing is in new query, not just this. This whole thing is a new query. It responds Christian Bale, because if you didn't send these two and only sent this as your query into the large language model, you would have actually gotten the question which character. That would be the same as just creating a new chat. This is what the context window is? That's the context window. This is the context window. So this is how large language models are taking memory. They don't. You ask a question, it responds, and it doesn't remember that you asked the question. It's just back in its old state. But the next time you ask a question, we kind of trick it by giving the entire. So if you have a conversation with a chatbot, you've you've asked been asking 10 questions and it's done 10 responses. When you ask the 11th question, the chatbot receives your entire transcript of 10 questions and 10 answers, plus the 11th question as input to actually answer the 11th question, 11th answer, right? So that is how this is what we call the context window, and the context window essentially tricks you, the user, into believing that the large language model has some kind of short-term memory, still short-term memory, not long-term memory, because if you abandon this chat, if you abandon this chat, you create a new chat and you come back to that new chat one week later and tell the LLM, "Do you remember what superhero I was talking about last week? The badge language model is going to say, "I don't, I don't know what you're talking about, right? So this is the issue. Now, yeah, it doesn't have memory, but why do we need these large language models to have memory in the first place? These large language models are stateless. That is, they do stateless, meaning they do not have long-term memory. They don't even have short-term memory, but we kind of fake the short-term memory with the context window exercise that we just talked about, right? So agents not having memory can be frustrating. Think about it, right? Think about an agent that is taking that is planning a trip for you, right? So maybe the last time the agent planned a trip for you, you told the agent that you don't like getting up too early in the morning during a vacation. You told the agent that you're a vegetarian. You told the agent that you like spicy food. Whatever, right?

So fast forward six months later, you're planning another another trip, and you tell the agent plan a three day trip to Tokyo, and your agent is asking all the questions all over again, or even worse, misses you. Remember telling the agent that you don't like getting up early in the morning during your vacation, but your agent doesn't remember that. Assumes that you can start your tour of the city at 6 a.m. and comes up with a big plan. You don't check on it particularly because you think that you've already told your agent, which would be the case if this was a physical. Agent, physical travel agent who works with you all the time, you would expect them to remember. But this particular travel agent is has amnesia. He or she does not remember anything more than five seconds, like a goldfish. So the problem is that this agent will come book a trip for you that essentially makes you has you dining in all kinds of restaurants that don't have vegetarian options, it starts your day at 6 a.m. in the morning. It things that you would have expected a travel agent to remember. So not having memory can be frustrating and most importantly very inefficient and sometimes very damaging, right? The easy thing is we can provide this memory, and we will do that by simple text injection. And let me show you an example of how we can actually endow an agent with memory by text injection. Right. So let's go through an example again. So here we have an agent. We have an agent with memory. Here is a text file called memory.txt, and memory.txt is currently a blank and empty text file. Right? That's it. Now think about an LLM again, large language model, and let's take the example where I tell the large language model first. The first prompt I give the large language model is I I'm lactose intolerant and I only drink oat milk. So I tell it I'm lactose intolerant. I only drink oat milk, and the large language model responds by saying, "Okay, I get that. Second, one week later, I come back to this large language model and I say, "Order a latte. Right. So again, this is a large language model. It doesn't have tools, so it can't really order a latte. But let's say we put it in an agent without memory, and we give the large language model the ability to order tools. Okay, so we we let's do that. Let's let's put a tool that says make coffee. Make coffee, and this tool can actually produce coffee. Right, that's my coffee. This is a robotic tool. It's a Python script that large language model can call. Again, we'll have an agent executor, and it can talk to the agent executor, and this agent executer can actually have this robotic coffee maker make coffee, right? So this is a large language model. Again, the same thing. Like this is the executor. So I say order latte, the large language model. This agent prompt tells me what tools are available, and using all of those tools, the large language model sends a JSON call, and this executer converts this JSON call into a function call and calls this robotic arm, and that robotic arm pushes coffee out. The problem is that without memory, order latte will basically make the LLM send a latte making instruction, make the robotic arm make latte in normal milk. It doesn't remember the fact that I told it that last week that I'm allergic. I'm allergic or I'm lactose intolerant and I can only drink oatmeal. So we'll have a problem. We need to endow this agent with some memory, and this is how we do this. We create a text file called memory.txt. In this example, that could be a database too, right? And this is an empty file right now. We say you are a helpful assistant. You have access to a called called save memory. It's called save memory, and so there is another function called save memory.

Def save memory.

Right? It's got a bunch, and that function will obviously write to memory.txt. Right? So, and that's another tool. So in this case, you have a tool called Save Memory plus Make Coffee. That's your Save Memory. That's your Make Coffee. Two tools. So you're a helpful assistant. You have access to two tools called Save Memory and Make coffee instruction. If the user mentions a personal preference, you must call safe memory to store it for future. And you need another instruction saying if the user asks you to make coffee, call the make coffee tool with this parameters and so on. Do not save casual conversations; only permanent facts. Okay. Now, and this memory.txt is in. Entirely paste it here. Okay, right now it's empty, so it's pasted here. It's empty. It goes to the element, and the first thing you say is, "I only drink oat milk. It knows the instructions. It says, "Oh my God! Now the user is telling me about a personal preference. So it call. It creates a JSON call to save memory, and says inside the JSON call parameter, it says this user only is lactose intolerant and only drinks oatmeal. So that

is sent to safe memory, and it's written down here. It says this user, this user, this user only drinks oat milk.

Okay, and this response noted. Thanks. Right, so it saves it, comes back, reports the LLM that it saved, and it's responding. Noted. Thanks. Next week, you come, you say order latte. It goes to the LLM. But remember the agent prompt. The agent prompt not only said this. The agent prompt pasted this text in here. So the agent prompt clearly told the LLM that this user only drinks oat milk, and with that instruction in play, the moment you say order latte, the JSON call that gets created here is a call to make coffee, and the JSON prompt clearly says make coffee with oat milk, and then you have your oat milk coffee. And for for this, you have get this percent of your oat milk latte is ready, right? So this is how memory works. Remember, the memory here was really again fake. The LLM never had memory. The LLM was never given memory. The LLM forgets that you had oat milk. You only have oat milk. The LLM forgets, but the system remembers. So the LLM still does not have memory, but the system Now has memory, right? We create the memory in the system. So this is an agent with memory. The LLM still does not. Now memory can. This is long-term memory. You could have a text file. You could have a little more of a structure. You could have a structured list. You could have it stored in a database. You could have multiple users, in which case you need to pull up the right user for the right time. You could do a whole bunch of things, right? But every single time, the moment you talk about memory, you are talking about some kind of a text injection into the prompt, right? Even if this was stored in a database, you query the database, take it back in plain English, and stick it back into the agent so that the agent LLM is instructed to remember. So they use something that was already remembered by the system. That's pretty much it. Now the reason why the reason why memory can be very useful. It's kind of obvious, but just to work with, we talked about personalization. This is essentially the computer or the agent understanding that I do not like to get up early in the morning during my vacations, or understanding that I like to have my drinks with oat milk, blah blah blah, that kind of stuff. Context continuity. So I could be working on a project. I could be submitting it to my manager. Two weeks later, my manager responds with the feedback. I want to start off right where I finished with, and therefore, some kind of a context memory that kind of gets the large language model up to speed would be really useful. Its efficiency, because I don't want to tell the large language model the same thing are repeated last week. Not only is it inefficient, but I might miss saying something, and I might get something that I don't like. And long-term reasoning. And so, if there are reasoning thoughts that we've made long-term, so we were trying to plan a project, and there was a lot of reasoning we did, and we don't want to repeat all that reasoning mechanism all over again today, right? We did we did a brainstorming session with the LLM. Me and the LLM did a whole bunch of brainstorming, and we concluded that this is a good way to design this building. But then, when I want to, when I get the feedback from my city, the permit process done, I don't want to go through the entire reasoning again. I want the LLM to be up to speed on its reasoning abilities from last week, so that I can continue the reasoning and make a better design this week too. We already talked about short term versus long term. Long term is anything that you store and you're able to pull back. Short term is usually the one that is. Available in the context window currently.

With all that said, these are what we call the agent's body. The agent's body, the agent's brain is a large language model, but the agent's body is all the tools. So we talked about how agents communicate with the tools, and we talked about how agents can borrow tools from an MCP server, and finally, we talked about how agents with memory can use the same set of tools like the coffee making mechanism much better and more efficiently without making a complete mess.

Transcribed by https://otter.ai
In this lab, we're going to build an intelligent credit card recommendation system for a fictional financial services company called Credit X. We will explore how to enhance an AI agent by giving it access to tools, which fundamentally changes how the agent can interact with external data sources. Instead of trying to fit all of our data into the prompt, we're going to give our agent the ability to query a database and retrieve exactly the information that it needs when it needs it. This approach is far more scalable and also more practical for real-world applications. Let's begin by understanding what we're building. Now we're going to connect our Credit X agent to a lightweight SQLite database called Credits.db. Now this is going to be storing both the customer credit card histories and also the catalog of available credit cards. To let the large language model interact with this data safely and also reliably, we will expose two focused tools. The first tool is called the Get Customer Record, and it's responsible for fetching an individual customer's financial profile from the database. The second tool is called the Get All Cards, and it retrieves the complete list of credit card products that Credex offers. Now, by structuring the system this way, we give the model controlled on-demand access to precise data instead of overloading the prompt with large tables. Now, this allows the agent to blend real customer information with its reasoning capabilities to generate accurate, personalized credit card recommendations. Now, in the first cell of this section, we define a constant called DB path, and we set it to the string credits DB. Now this is going to be the file name of our SQLite database, and this database path name will be used by our tools whenever they need to connect to the database and run the queries. Now by defining this as a constant at the top, we make our code cleaner and also easier to maintain. If you ever needed to change the database location, we would only need to update this one single line. Now we move on to writing the tools that our agent is going to be using. So in the next cell, this is where we create our first tool, which is the get customer records function. Now this tool is responsible for pulling a customer's profile directly from the customers table in our SQLite database. Notice that we use a special decorator at the top of the function. So, as a quick recap, a decorator in Python is a feature that tells us to wrap a function with additional behavior. In this case, the tool decorator from LangChain registers our get customer record function as a callable tool that the agent can invoke during its reasoning process. When the agent decides that it needs customer information, it can call this tool by name. Now, looking at the function itself, it takes a single parameter called customer name, and this can be either the full name or a partial name of the customer that we are looking for. We then create a cursor object, which we essentially use to execute the SQL queries against the database. Now, the query that we are creating uses a SELECT statement to retrieve all of the columns from the customers table, where the name matches our search term. Now, why we like to do it this way is because it allows us to do some sort of a partial matching, so it means that if someone searches for just Paul, it's going to find Paul Rodriguez or any other customer whose name contains Paul. The limit one essentially ensures that we only get one result back, even if multiple customers match. Now, after executing the query, we fetch the first matching row, and if no customer is found, the function returns a helpful message indicating that there is no record found for that name. If a customer is found, we map each column from the database row to a readable key name. The columns include customer ID, name, age, income, credit score, past defaults, and all of the spending categories like travel spend, fuel spend, shopping spend, grocery spend, restaurants, medicine, rent, utilities, and also entertainment spend. Next up, we also create a dictionary that pairs each column name with its corresponding value from the database row, and we return this structured data.

Now, this lets the agent reason over the actual customer information, rather than relying solely on what might be in the prompt. Now, it is worth mentioning that in this implementation, we are using a static SQL query where the structure of the query is predetermined and only the customer name parameter changes. However, there are more advanced scenarios where you might want the large language model to dynamically generate these SQL queries based on the user's natural language request. Now, for example, imagine a user asking something like, "Hey, can you show me all customers in California who spend more than $2,000 on travel and have a credit score?" Above 750. Now, in such a case, the large language model would need to understand the intent, construct the appropriate SQL query with the right table joins, where the WHERE clauses and also the conditions, and then also execute it. Now, this dynamic SQL generation is powerful, but it does require careful implementation to prevent security issues like SQL injection attacks, and it's also beyond the scope of what we need for our current credit card recommendation use case. Now, continuing on, let's go to this cell where we create our second tool called the Get All Cards. Now, this tool gives the agent access to the full catalog of credit cards that are stored in the credit cards table of our SQLite database. Now, like our first tool, we use the tool decorator to register this function so that the agent can call it during its reasoning process. Now, the function takes an optional parameter with a default empty string. Now, this parameter exists because the tool interface expects some input, but our query does not actually need any parameters since we are fetching all cards regardless of any criteria. Now inside this function, we open a database connection just as we did before. We create a cursor and then we also execute a simple select query that retrieves the card name, the benefit category, and also the description from the credit cards table. We fetch all of the rows returned by this query, and then we close out the database connection. Now the function then transforms the raw database rows into a list of dictionaries, where each dictionary represents one credit card with keys for the card name, benefit category, and also the description. Now, by exposing this as a tool through the decorator, the agent can dynamically fetch product information whenever it needs to compare options or recommend the best match for a customer, all without us having to embed large tables directly into the prompt. Now, continuing on, so in the following cell here, we create a simple list called tools that contain both of our tool functions, the get customer record and also the get all cards. Now this list will be passed to our agent during creation, telling it which tools are available for it to use. The agent will examine the names and descriptions of these tools to determine when and also how to use them during conversations. Now we arrive at a crucial cell where we create the core Credit X agent itself. Now we use the create agent function, and within this function we pass it three important components. The first component is our language model, which we pass through the model parameter. This is the GPT-4 model that we configured earlier, and it acts as the reasoning engine that powers our agent's intelligence. The second component is going to be our tools list, which tells the agent what external capabilities it has access to. The third, and perhaps the most important component is the system prompt, which essentially establishes the rules and personality for how our assistant should behave. Now let's examine the system prompt in more detail because it does shape the entire behavior of our agent. So the prompt begins by telling the agent that it is Credit X's credit card recommendation assistant, and then it outlines its specific responsibilities. So the first responsibility, as we see over here, is that if the Get Customer Record tool returns the customer data, the agent should summarize that information and recommend the most suitable cards. However, there is an important constraint: the agent should not recommend a credit card if the customer's credit score is below 750. The second responsibility addresses the case when a customer does not exist in a database.

So in this scenario, the agent should ask three to four friendly questions to learn about the customer's spending behavior, lifestyle, and also income. The third responsibility is it tells the agent to use the get all cards tool to understand the available products when needed, so it can make informed recommendations. And then, lastly, the fourth responsibility here establishes another critical business rule. If a customer has defaulted two times, the agent should not recommend a credit card to them. And then, finally, the prompt instructs the agent to keep the replies short, helpful, and also clear. Now, this structure ensures that the agent responds consistently, stays grounded in actual database information, and also delivers concise, helpful credit card recommendations. In the next cell, this is where we define a wrapper function called the Credit X agent that makes it easy and also clean to interact with our agent. Now this function takes a single parameter, which is going to be the user message, which is whatever the customer types to our system. And inside the function, we call the agent invoke and pass it as a dictionary with the message's key. The value is a list containing. A tuple where each element is the string user indicating who sent the message, and the second element is the actual message content. The invoke method triggers the agent's reasoning process, which may involve calling tools, analyzing results, and also formulating a response. The function then also extracts and also returns just the content of the last message in the result, which is the assistant's response. Now, by abstracting away the underlying message format and invocation details, this wrapper makes it effortless to call the agent from other parts of an application, while also keeping the interaction smooth, consistent, and also easy to reuse. We're now ready to test our tool-enabled agent with several test cases that demonstrate different scenarios. Now, in the first test case, this is where we examine what happens when a customer's history is present in the database, but they don't qualify for a credit card. Now, what's kind of happening over here is that we create a user query where someone named Paul introduces himself and asks about all of the credit card options that are available. We pass this query to our credit X agent function and print both the query and the response. Now this is what we kind of get back here. Now as you can see, the agent first uses the get customer record to look up Paul in the database, it finds Paul Rodriguez's record and discovers some concerning information. So Paul has a credit score of 636, which is below our threshold of 750, and Paul has also defaulted twice. Based on the business rules that we define in our system prompt, the agent cannot recommend a credit card to Paul. The agent's response acknowledges Paul mentions his credit history, including the two defaults and also the low credit score, and also politely explains that it cannot recommend a credit card at this time. The agent over here also offers to help with questions or advice on improving his credit score, maintaining a helpful tone, while also delivering some disappointing news. Moving on to our second test case, this is where we have another scenario where the customer history is present in the database, but a credit card is still denied. This time, a customer named Joseph asks about credit card options. The agent uses its tool to fetch Joseph Anderson's record from the database. Now, interestingly, Joseph has a good credit score of 818, which is well above our 750 threshold, and also has a substantial income. However, the database shows that Joseph has defaulted twice in the past. Now, according to our business rules, customers who have defaulted twice cannot receive credit card recommendations, regardless of their current credit score. The agent correctly applies this rule and explains to Joseph that despite his good credit score and substantial income, it cannot recommend a card due to two past defaults. Now, this test case demonstrates how the agent can weigh multiple factors and apply business logic even when some indicators are actually positive. Continuing on to our third test case. So this case shows the happy path where a customer qualifies for a credit card recommendation. Now a customer named Jessica introduces herself and asks about all of the available options.

Now the agent retrieves Jessica Miller's record from the database and finds favorable information. She has an excellent credit score of 828 and zero past defaults. Jessica's spending data shows significant activity across multiple categories. Based on this profile, the agent provides a comprehensive and personalized recommendation. Now, when you examine the output, you'll see that the agent recommends several specific cards that are tailored to Jessica's spending habits. It suggests the Shop Smart card because it offers five percentage cashback on all shopping purchases, which aligns with Jessica's high shopping spend. It also recommends the Grocery Saver card for its discounts and cashback at major grocery chains, matching her grocery expenses. The Travel Pro card is suggested for its lounge access and travel discounts, beneficial given Jessica's travel spending, we also see the Fuel Flex card, which is recommended for its cashback and also surcharge waivers at gas stations, corresponding to her fuel expense. And then we also have the Movie Max card. This is suggested for its entertainment perks like buy one get one free offers on movies and concerts, complementing her entertainment spending. The agent explains that these cards are tailored to maximize the benefits based on Jessica's spending patterns and also offers to provide more information. Now, this test case beautifully demonstrates how the agent combines tool retrieved data with intelligent reasoning to provide genuinely personalized recommendations. In our fourth and final test case, we explore what happens when a customer's history is absent from the database. A new customer named Sean introduces himself and asks about the credit card options. When the agent calls the Get Customer Record tool with Sean's name, it receives a response indicating that. No customer record was found. The agent recognizes this situation and follows the instructions in its system prompt for handling new customers. Now, instead of making assumptions or just having some sort of a generic recommendation, the agent first starts by greeting Sean warmly and explains that since it could not find his record, it would love to learn more about him to recommend the best options. The agent then asks a series of thoughtful questions. So it asks about Sean's main spending categories, giving examples like travel, dining, and groceries. It asks about his preferred lifestyle or any specific benefits that he's looking for in a credit card, it inquires about his approximate annual income and also asks how often he travels. The agent concludes by explaining that these answers will help it suggest the most suitable credit cards. Now, this approach allows the agent to gather the information that it needs to make personalized recommendations, even for customers who have no prior history with Credit X. However, there is an important limitation that's revealed in the follow-up interactions with this test case. When Sean responds with his preferences in a subsequent message, something unexpected happens. Even though Sean provides information about his spending on groceries, his preference for cashback, his income of $100,000, and also his interest in lounge access. The agent does not connect this information to the previous message. Instead, it asks the same introductory questions again as if it never spoke with Sean before. Now this happens because our current agent implementation does not have memory. Now that's what all of this is what we see over here. Now, each call to the agent is treated as an isolated interaction with no awareness of what was discussed in previous messages. The agent cannot build upon the conversation or accumulate information across multiple turns. Now, this limitation highlights a fundamental gap in agents that only have tools but lack memory. Now, while tools give the agent powerful capabilities to access external data, without memory, the agent cannot maintain conversational context. Every interaction starts fresh, which creates a frustrating experience for users who expect the natural flow of a conversation where earlier statements are remembered and built upon, this is precisely why memory becomes such a critical component to add to our agent architecture, which we'll explore in the next section.

Transcribed by https://otter.ai
Let's now discuss the limitations of relying only on tools to enhance our AI agent, and explore how memory can overcome these challenges. Throughout our test cases, we observed that while tools provide powerful capabilities for data retrieval, there are important limitations when tools are the only enhancements that we make to our agent. The first limitation is that tools work extremely well when customers have pre-existing credit histories or transaction data to anchor recommendations. We saw this with Jessica, where the agent could retrieve her complete spending profile and also make excellent personalized suggestions. However, tools alone struggle significantly with new two-credit customers. While the agent can ask follow-up questions, those questions remain shallow and fail to gather full context because the agent cannot track what was already asked or being answered. The second limitation became evident in our fourth test case with Sean, the agent cannot naturally recall earlier answers or preferences across multiple turns of conversation. This causes repetitive or disjointed conversations where the agent asks the same questions multiple times, unaware that the customer has already provided that information. Each message is treated in complete isolation, as if it were the very first message interaction. The third limitation is that the agent builds weak customer profiles for new users, because each message is processed independently instead of being a part of a evolving narrative. The agent cannot piece together information that customers provide incrementally. A customer might share their spending habits in one message, their income in another, and also their lifestyle preferences in a third. But the agent has no way to compile these into a coherent profile. The fourth limitation is that without the ability to accumulate information gradually, the agent produces less accurate and less personalized recommendations for new customers. The quality of recommendations depends heavily on having complete information, but tools alone provide no mechanism for building that complete picture over time. Now let's consider how memory overcomes these limitations. Now memory retains user-provided details across turns or sessions, enabling the agent to reason with continuity instead of starting fresh each time. When a customer shares information in one message, that information persists and can be referenced in future exchanges within the same conversation. Memory supports deeper, multi-turn conversations where the agent can build context step by step. This is especially valuable for new-to-credit customers who do not have database records, the agent can ask the initial set of questions, store the answers, ask follow-up questions based on those answers, and continue this process until it has enough information to make a quality recommendations. Now, memory helps form richer and also more complete customer profiles, even when initial data is minimal. Instead of being limited to what is in the database, the agent can construct a detailed understanding of the customer through conversation. Memory allows the agent to adapt recommendations dynamically as more information emerges throughout the interaction, early in a conversation, the agent might have only partial information. But as the customer shares more details, the agent can refine and also improve its suggestions. And then finally, memory helps to elevate the system from a reactive answer bot to a genuinely context-aware decision-making assistant. Now, this transformation is crucial for domains like credit card recommendations, where understanding the customer unfolds gradually through natural conversation rather than through single comprehensive query. So, this is why combining tools with memory creates a far more capable and user-friendly AI system, which we will then explore in the next section.

Transcribed by https://otter.ai
In this lab, we're going to build an intelligent credit card recommendation system for a fictional financial services company called Credit X. Now, we will be exploring how to enhance an AI agent by combining two powerful capabilities: tools that allow the agent to fetch data from a database and memory that enables the agent to maintain context across multiple conversational turns. This combination is essential for creating truly conversational AI systems that can handle complex multi-step interactions with users. Now, before we dive into the implementation, let's understand why memory is such a critical component for our recommendation system. Imagine that you're talking to a customer service representative about getting a new credit card. Now you would expect that representative to remember what you said at the beginning of the conversation as you continue talking. If you told them that what your name is and also your spending preferences early on, you would not want to repeat that information every time that you ask a follow-up question. Now, this is exactly the challenge that we face with AI agents. By default, each message sent to a language model is treated in isolation. The model does not automatically remember what was said in previous messages to create a natural, flowing conversation, we need to explicitly manage and also maintain that conversational history, which is what our memory system will accomplish. Let's begin by understanding the architecture that we're building. So we are introducing a full conversational memory layer that enables our Credit X agent to maintain context across multiple turns within each user session. Now, this memory layer does several important things. First, it tracks a dedicated chat message history for every individual session, meaning each customer gets their own separate conversational thread. Second, it automatically formats past messages into the structure that's required by the agent, so the language model can understand and also reason over the entire conversation history. Now, this design allows our agents to have coherent multi-turn conversations where it can build upon information gathered in earlier exchanges. Now let's move on to the first cell, and in this section we can see that we are setting up the memory storage mechanism. So we create a dictionary called memory store, and this is going to serve as our in-memory storage for the conversation histories. The keys in this dictionary will be the session identifiers, and also the values in the chat message history object that's going to contain the actual conversation messages. Now the get session history function is essentially going to be a helper function that retrieves the conversation history for a given session. When this is called, it first checks if a history already exists for that session ID. If the session ID is not found in our memory store, meaning that this is a new conversation, the function creates a fresh chat message history object for that session, and it adds it to the store. If the session already exists, it simply returns the existing history. Now, this automatic initialization ensures that we can smoothly handle both the new and also the returning users without any additional setup code. Now, proceeding to the next cell, this is where we define a helper function called serialize history. Now, this function is responsible for converting our stored conversation messages into a clean, standardized format that our agent can work with the function takes a list of messages and iterates through each one. For each message, it checks whether the message is a human message, which represents something the user said, or an AI message, which represents something that the assistant responded with. Now, based on this, it creates a dictionary with a role field set to either user or assistant, and a content field containing the actual conversation text. The function collects all of these formatted dictionaries into a list and it returns it. Now, this standardization is crucial because different parts of our system may store messages in different formats, but our agent needs them in a consistent structure to reason over the conversation properly. Continuing on, now we arrive at the heart of our memory-enabled agent. In this cell, we define the credit X agent memory function, which serves as the central entry point for all of our interactions with the recommendation system.

Now, this function takes two parameters: the user message, that's what we see over here, which essentially contains what the customer had just said, and we also here have a session ID that defaults to the string default, but it should be set to a unique. Identifier for each customer conversation. Now the function begins by calling the get session history with the provided session ID to retrieve or to create the conversation history for the session. Next, it adds the user's current message to the history using the add user message method, and this ensures that even before we get a response, we have recorded what the user said. Now the function then builds the complete message sequence by calling our serialize history, and it does this on all of the messages in the history. This gives us a formatted list of all of the previous exchanges plus the current user message. We then invoke our agent with this complete message history, and this is essentially going to be allowing the model to see the entire conversation context when formulating its response. After the agent generates a response, we extract the assistance message from the result, and we add it to our history using the add AI message. Now, this completes the memory loop, ensuring that both the user messages and also the assistant responses are preserved for future turns. Now, before we run our test cases, I want to note that all conversations related to a single customer will happen within each session. Now, each session will have a unique session identifier in the format SID followed by three digits such as SID 001 or SID 002, and this allows us to track multiple separate conversations and see how memory behaves differently across the different sessions. Okay, so now let's run our first test case to see the memory-enabled agent in action. Now, in this scenario, we have a new customer named Sean who's not in our database, and importantly, this customer chooses not to share their personal information when asked. We start by sending a message where Sean introduces himself and asks about the credit card options. Now, when you execute the cell, you'll see that the agent responds by acknowledging that it could not find Sean's record. Now, because Sean is a new customer with no historical data, the agent follows its programmed behavior and asks a series of friendly questions to learn more about Sean's spending habits, lifestyle preferences, approximate annual income, and also the travel frequency. The agent explains that these answers will help it suggest the most suitable credit cards. Now, continuing in the same session, we now simulate Sean responding that he does not want to share his personal information. Notice here that we are calling the credit X agent memory function, and this is with the same session ID SID 001, which means that this message is a part of the same ongoing conversation. When you run the cell, something interesting happens now because the agent has memory of the previous exchange. It understands the context of the conversation. The agent recognizes that Sean has declined to share personal details. So instead of asking more questions or getting stuck, it pivots its approach. The agent now lists all of the available credit card options without trying to personalize the recommendation. It represents cards like the ShopSmart card with the 5% cashback on shopping. It also recommends here the grocery saver card for the grocery discounts. We also show the Dine Plus card for the restaurant savings. We have the Travel Pro for the lounge access and discounts on all of the domestic flights. The Fuel Flex card for cash back at gas stations. Movie Max for buy one get one on movies, and also utility savers, or cash back on rents and stuff. So the agent also invites Sean to share their preferences later if Sean decides to change their mind. Now this demonstrates how the memory system allows the agent to adapt its behavior based on the conversation flow. Now moving on to the second test case, this is where we explore what happens when a new customer actually does share their requested information. So we start with a completely fresh session ID with the identifier SID 002, which means that the memory from the previous test case does not carry over. Again, we have Sean introducing himself and asking about the credit card options.

The agent responds similarly to before, recognizing that Sean is not in the database and asking questions about spending categories, lifestyle preferences, annual income, and also the desired card benefits. Now, in this cell, we can see that Sean now provides some of his preferences. He indicates that he spends about $1,000 on dining and travel, and also that he prefers cashback rewards. Now, because we're using the same session ID SID 002, the agent has full memory of the previous exchange where it asked questions. It can now connect Sean's answers to those questions and provide a personalized recommendation. The agent suggests the Dine Plus card because it offers up to 20% off at participating restaurants, which aligns well with Shawn's dining expenses. It also recommends the Travel Pro card for its lounge access and also travel discounts, which matches Sean's travel spending. The agent notes that these cards align with Shawn's spending habits and also cashback preferences, And also offers to provide more information if needed. Now, this test case demonstrates how memory enables the agent to gather information incrementally across multiple turns and also synthesize it into a coherent recommendation. Moving on, this is where we get to our third test case. Now, this examines a scenario where the customer's history is already present in our database. Now again, we start with a new session ID SID 003, and we have Paul Rodriguez introduce himself and asking about credit card options. Now, when the agent processes this request, it first uses its database tool to look up Paul Rodriguez's information. The agent finds Paul's record and discovers that he has a credit score of 636, and that's what it found over here. And also that Paul has defaulted twice in the past. Now, based on the business rules that we established, which prohibit recommending credit cards to customers with credit scores below 750 or those who have defaulted twice, the agent politely declines to recommend a card. It explains the reason and offers to help with other questions or provide assistance with improving the user's credit score. Now this shows how the agent combines tool-based data retrieval with memory-based conversation management. Now in our fourth test case, we have a new customer named Ragov who introduces some of his preferences directly in his initial query. He asks the agent to compare the best option for him and also lists all the premium travel cards starting with the new session ID SID 004. Now the agent first attempts to look up Ragov in the database and finds no record. Now, because Ragov is new, the agent asks clarifying questions about spending categories, travel frequency, income, and also the desired benefits. However, the agent also directly addresses the second part of Ragov's query by listing the premium travel cards that are available, specifically mentioning the Travel Pro card with its lounge access and also discounts on domestic flights, Amtrak, and also Metro. Now this demonstrates how the agent can handle multi-part queries and also provide partial answers while gathering more information for complete personalization. Our fifth and final test case is particularly interesting because it reveals an important limitation of our current system. Now, in this scenario, we have a new customer who happens to share the exact same names as somebody already in our database. So, a new person named Joseph Anderson starts a conversation with session ID SID 005. Now, when the agent looks up this name. It finds the existing Joseph Anderson in the database who has a good credit score of 818, but has defaulted twice. The agent denies the credit card recommendations based on these defaults. Now, in that subsequent exchanges with this session, the customer repeatedly tries to explain that they are not the person in the database, and that there must be some sort of a confusion. They send messages like, "But I am not that person. Perhaps you're confusing me with someone else. The agent, however, keeps retrieving the same database record because the name matches, and it continues to reference the existing customer's default history. Now, even when the customer becomes increasingly frustrated and explicitly states multiple times that there must be some sort of a mix-up. The agent still struggles to handle the ambiguous situation. The customer eventually expresses significant frustration, and the agent finally acknowledges the confusion and asks for more information about spending habits, lifestyle, and also income to resolve the situation.

However, even after this, when the customer continues to insist on the identity confusion, the agent reverts back to citing the database record for Joseph Anderson. Now, this test case powerfully illustrates a fundamental limitation on relying solely on tools and memory. While memory can store facts and tools can retrieve data. Neither component can truly reason about ambiguous or conflicting information in a sophisticated way. Now, this concludes the implementation portion of our single agent system with tools and memory. You have seen how to set up session-based memory storage, how to serialize conversation history into a format that the agent can use, how to build a memory-enabled agent function that maintains context across turns, and also how the agent behaves in various scenarios, including new customers who share or withhold information, existing customers with database records, and also edge cases involving identity confusion.

Transcribed by https://otter.ai
Let's now discuss the limitations that we observe in our single-agent system that relies on tools and memory, and explore how reasoning capabilities can overcome these challenges. Now, throughout our test cases, we encountered several important limitations of relying only on tools and memory, the first limitation is that while memory can store facts from conversation, it cannot determine which pieces of information are actually relevant to the current task. Now, this can lead to cluttered or misused context where the agent has access to information but does not know how to prioritize or apply it effectively. The second limitation is that memory improves continuity between conversation turns, which is valuable, but it does not inherently help the agent plan ahead, strategize, or choose the best next question or action. The agent can remember what was said, but it cannot necessarily use that memory to form a coherent multi-step strategy for helping the customer, the third limitation became evident in our test cases. The agent may remember details, but it still fails to synthesize them into deeper insights or structured decisions. Having information is not the same as understanding what to do with that information. Memory stores data, but it does not automatically generate wisdom or judgment from that data. The fourth limitation, which we saw dramatically in our fifth test case with Joseph Anderson, is that memory alone cannot handle ambiguous, incomplete, or conflicting information. When the customer insisted that they were not the person in the database. The agent had no mechanism to reason through this conflict. It simply kept retrieving the same database record because the name matched, causing the agent to stall and produce inappropriate responses despite the customer's repeated objections. The fifth limitation is perhaps the most fundamental. Storing more information does not automatically improve performance. Without sophisticated reasoning capabilities, the agent may still respond reactively rather than intelligently, simply pattern matching to previous experiences instead of truly understanding and also solving the problem at hand. Now let's consider how reasoning capabilities can overcome these limitations. First, reasoning allows the agent to determine which stored memories actually matter for the current task. Instead of treating all information equally, a reasoning-enabled agent can evaluate relevance and focus on contextually appropriate details, leading to more targeted and also useful responses. Second, reasoning enables the agent to plan multi-step interactions. So rather than just responding to the immediate message, the agent can think about what questions to ask in what order, and also how to strategically gather missing details. Now this transforms simple question-answer exchanges into intelligent guided conversations. Third, reasoning allows the agent to synthesize remembered information into coherent evidence-based recommendations. Instead of simply recalling facts, the agent can weigh different factors, consider trade-offs, and also construct well-justified suggestions that account for the full picture of what it knows about the customer. Fourth and critically important, given what we observe in our test cases, reasoning enables the agent to handle ambiguity. So, when faced with incomplete or conflicting information, a reasoning-enabled agent can infer likely explanations, generate hypotheses about what might be happening, and also choose the best next move, rather than passively recalling facts that may not apply to the situation. And then finally, reasoning transforms the agent from what we might call a memory-dependent listener into a proactive problem solver. Instead of just storing and retrieving information, the agent becomes capable of deeper analysis, more nuanced understanding, and also smarter decisions that account for the complexity of real-world interactions. Now, this evolution from simple memory to sophisticated reasoning represents the next frontier in building truly intelligent AI systems that can handle the messy, ambiguous, and also complex scenarios that customers bring to services like credit card recommendations. The combination of tools for data access, memory for conversation continuity, and reasoning for intelligent decision making creates a powerful foundation for building AI assistants that can genuinely help users navigate complex decisions.

Transcribed by https://otter.ai
